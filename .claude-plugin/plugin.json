{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "trusted-loop",
  "displayName": "Trusted-Loop Mode",
  "version": "0.1.0",
  "description": "Claude Code plugin that gates an AI coding agent's Stop with an independent LLM-judge ensemble — keep an unattended agent working until tests/build prove the task is done, not just when it claims so.",
  "keywords": [
    "claude-code",
    "claude-code-plugin",
    "claude-code-hooks",
    "stop-hook",
    "ai-coding-agent",
    "ai-agents",
    "autonomous-agents",
    "agentic-workflow",
    "unattended-agents",
    "agent-verification",
    "completion-verification",
    "llm-as-judge",
    "llm-judge",
    "judge-ensemble",
    "llm-evaluation",
    "definition-of-done",
    "anthropic",
    "openai",
    "developer-tools",
    "python"
  ],
  "author": {
    "name": "Mehmet Can Farsak",
    "url": "https://github.com/mehmetcanfarsak"
  },
  "homepage": "https://github.com/mehmetcanfarsak/Trusted-Loop-Mode",
  "repository": "https://github.com/mehmetcanfarsak/Trusted-Loop-Mode.git",
  "license": "MIT",
  "commands": "./agents/claude-code/commands",
  "hooks": "./agents/claude-code/hooks/hooks.json",
  "userConfig": {
    "MAX_ITERATIONS": {
      "type": "number",
      "title": "Max iterations",
      "description": "Hard cap on block→continue iterations before the loop finalizes and allows stop.",
      "default": 12
    },
    "WALL_CLOCK_MINUTES": {
      "type": "number",
      "title": "Wall-clock budget (minutes)",
      "description": "Wall-clock budget (minutes) after which the loop finalizes regardless of judges.",
      "default": 120
    },
    "STUCK_LIMIT": {
      "type": "number",
      "title": "Stuck limit",
      "description": "Allow stop after this many iterations with no observable change (evidence hash stable).",
      "default": 3
    },
    "VERIFY_WITHOUT_GOAL": {
      "type": "boolean",
      "title": "Verify without an explicit goal",
      "description": "If true, with no goal armed, infer one from the user's last request instead of allowing stop.",
      "default": false
    },
    "NOTIFY_WEBHOOK": {
      "type": "string",
      "title": "Notification webhook URL",
      "description": "Optional URL POSTed on stuck / finalize / judge-error so unattended can mean 'notify me'.",
      "default": "",
      "sensitive": true
    }
  }
}
