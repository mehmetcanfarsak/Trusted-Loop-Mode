---
description: Show Trusted-Loop state — goal, iteration, criteria, checks, judges
allowed-tools: Bash
---

Print the live loop state. Use this to recover the goal after a compaction.

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/core/manage.py" status
```

Relay the output to the user verbatim.
